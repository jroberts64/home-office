// Public config. NO secrets here — this file ships to every visitor.
// After `sam deploy`, copy the ApiEndpoint output into API_BASE below.
window.HOME_OFFICE_CONFIG = {
  API_BASE: "https://REPLACE_ME.execute-api.us-east-1.amazonaws.com",
};
