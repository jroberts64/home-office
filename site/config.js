// AUTO-GENERATED at deploy time by deploy/deploy.sh from the CloudFormation
// ApiEndpoint output. This committed copy is just a placeholder so the page
// loads locally; the real API_BASE is written during deploy.
// NO secrets here — this file ships to every visitor.
window.HOME_OFFICE_CONFIG = {
  API_BASE: "https://REPLACE_ME.execute-api.us-east-1.amazonaws.com",
};
